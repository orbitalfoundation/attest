name = "attest"
horizon = "2027"
repo = "anselm/attest"
code = "local"
deploy = "exe:attest"
status = "active"        # active | parked (struck through on the dashboard) | archived (hidden)
autodeploy = false
notes = "manual deploy: deploy/deploy.sh; push != deploy; staging at https://attest.exe.xyz (pass 1, 2026-09-23); local run: PORT=8100 npm start; e2e: npm run e2e"
