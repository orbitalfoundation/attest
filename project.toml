name = "attest"
horizon = "2027"
repo = "orbitalfoundation/attest"
code = "local"
deploy = "none"
status = "active"        # active | parked (struck through on the dashboard) | archived (hidden)
autodeploy = false
notes = "working name; attestations + emitter scoring service, separate from the desk; under orbital"
